@using CodingEvents.Models;
@model List<Event>

<h1>Coding Events</h1>
<div>
    <a asp-controller="Events" asp-action="Add">Add Event</a>
</div>

@if (Model.Count == 0)
{
    <p>No events yet!</p>
}
else
{
    <table class="table">

        <tr>
            <th>Id</th>
            <th>Event</th>
            <th>Category</th>
            <th>Description</th>

            <th>Edit</th>
            <th>
                Remove
            </th>
        </tr>
        @foreach (Event item in Model)
        {
            <tr>
                <td>@item.Id</td>
                <td><a asp-controller="Events" asp-action="Detail" asp-route-id="@item.Id">@item.Name</a></td>
                <td>@item.Category.Name</td>
                <td>@item.Description</td>
                <td>
                    <form method="get" action="/events/edit">
                        <button class="btn btn-success" type="submit" name="eventId" value="@item.Id">Edit</button>
                    </form>
                </td>
                <td>
                    <form method="post" action="/events/">
                        <input type="hidden" name="eventId" value="@item.Id">
                        <button class="btn btn-danger" type="submit">Remove</button>
                    </form>
                </td>

            </tr>
        }
    </table>
}